# Poll Manager

- Assignment project for DAT250

## Build and execute the monorepo project

This will build the frontend application and past it into the static folder in the backend project,
so that the frontend will be hosted and available under `localhost:8080` once the backend is
started.

```bash
gradle copyWebApp
gradle bootRun
```
